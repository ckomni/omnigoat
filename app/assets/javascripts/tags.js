$(document).ready(function(){
  $(".panel-container[data-posts]").each(function(index){
    var container = $(this)
    var posts = container.attr("data-posts")
    var size = container.attr("data-fraction")
  })

})
